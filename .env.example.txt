# Database (MongoDB)
# Replace the value below with your MongoDB connection string. Do NOT commit
# real credentials to your repository. Keep this file as an example only.
DATABASE_URL="YOUR_MONGODB_URI"

# NextAuth / OAuth
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"
NEXTAUTH_SECRET="your-nextauth-secret"
NEXTAUTH_URL="http://localhost:3000"

# App URL used client-side
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# UploadThing (File uploads) - Get these from https://uploadthing.com/dashboard
# Use placeholders in the example file
UPLOADTHING_SECRET="YOUR_UPLOADTHING_SECRET"
UPLOADTHING_APP_ID="YOUR_UPLOADTHING_APP_ID"

MUX_TOKEN_ID="YOUR_MUX_TOKEN_ID"
MUX_TOKEN_SECRET="YOUR_MUX_TOKEN_SECRET"

# Email Configuration (for notifications and PDF export)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="your-email@example.com"
SMTP_PASS="YOUR_SMTP_PASSWORD"

# WebRTC Configuration (for live streaming)
# You can use services like Agora, Twilio Video, or LiveKit
WEBRTC_API_KEY="YOUR_WEBRTC_API_KEY"
WEBRTC_SECRET="YOUR_WEBRTC_SECRET"

# Real-time Communication (WebSocket server URL)
WEBSOCKET_URL="ws://localhost:3000"

# PDF Generation Service
PDF_API_KEY="YOUR_PDF_SERVICE_API_KEY"

# Stripe (Payment Processing) - Add your keys to your real environment (do NOT commit)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="YOUR_STRIPE_PUBLISHABLE_KEY"
STRIPE_SECRET_KEY="YOUR_STRIPE_SECRET_KEY"
STRIPE_WEBHOOK_SECRET="YOUR_STRIPE_WEBHOOK_SECRET"

# (optional) NODE_ENV is usually set by the runtime; you can set it for local testing
NODE_ENV="development"

# Gmail example (do NOT commit real credentials)
GMAIL_USER="your-email@example.com"
GMAIL_PASS="YOUR_GMAIL_APP_PASSWORD"

AGORA_APP_ID="YOUR_AGORA_APP_ID"
AGORA_APP_CERTIFICATE="YOUR_AGORA_APP_CERTIFICATE"
